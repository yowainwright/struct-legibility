#!/usr/bin/env bash
# struct-legibility-managed-hook
set -euo pipefail

repo_root="$(git rev-parse --show-toplevel)"
cd "$repo_root"

nub run typecheck
nub run test:unit
nub run overrides:check
git diff --check
